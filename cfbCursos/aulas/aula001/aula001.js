// VAR LET e CONST

let nome = 'gabriel' // variável que só funciona no escopo em que foi declarada

var nome2 = 'xesq' // variável global que funciona em todos os escopos e sofre o HOISTING

const nome3 = 'mecxefe' // Variável em uqe o valor não pode ser alterado

console.log(nome)